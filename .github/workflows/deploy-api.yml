name: "Deploy API and Dependencies"

on:
  push:
    branches:
      - main
  pull_request:
    breaches:
      - main

jobs:
  
  # Deploy dependencies
  deploy-database:
    uses: NateMartes/epa/.github/workflows/deploy-db.yml@main
    
  deploy-kafka:
    uses: NateMartes/epa/.github/workflows/deploy-kafka.yml@main