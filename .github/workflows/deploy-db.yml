name: "Terraform Apply Database"

on:
  push:
    branches:
      - main

jobs:
  terraform-apply:    
    name: "Terraform Apply Database"
    runs-on: ubuntu-latest
    environment: production
    
    env:
      TF_CLOUD_ORGANIZATION: "epa-terraform"
      TF_API_TOKEN: "${{ secrets.EPA_TF_API_TOKEN }}"
      TF_WORKSPACE: "terraform-github-actions"
      CONFIG_DIRECTORY: "./database/terraform"
      
    permissions: # granular permissions
      # so GitHub can check out this repo using the default github.token
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Upload Configuration
        uses: hashicorp/tfc-workflows-github/actions/upload-configuration@v1.0.0
        id: apply-upload
        with:
          workspace: ${{ env.TF_WORKSPACE }}
          directory: ${{ env.CONFIG_DIRECTORY }}

      - name: Create Apply Run
        uses: hashicorp/tfc-workflows-github/actions/create-run@v1.0.0
        id: apply-run
        with:
          workspace: ${{ env.TF_WORKSPACE }}
          configuration_version: ${{ steps.apply-upload.outputs.configuration_version_id }}

      - name: Apply
        uses: hashicorp/tfc-workflows-github/actions/apply-run@v1.0.0
        if: fromJSON(steps.apply-run.outputs.payload).data.attributes.actions.IsConfirmable
        id: apply
        with:
          run: ${{ steps.apply-run.outputs.run_id }}
          comment: "Apply Run from GitHub Actions CI ${{ github.sha }}"
          
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install Dependencies
        run: pip install -r ./database/requirements.txt
        
      - name: Setup Terraform CLI
        uses: hashicorp/setup-terraform@v3
        with:
          cli_config_credentials_token: ${{ secrets.EPA_TF_API_TOKEN }}
          
      - name: Init Database
        run: |
            cd ${{ env.CONFIG_DIRECTORY }}
            
            terraform init
            echo $(terraform output -raw mongo_db_cluster_dns_name)
            export EPA_MONGODB_HOSTNAME=$(terraform output -raw mongo_db_cluster_dns_name)
            export EPA_MONGODB_PORT=27017
            export EPA_MONGODB_USERNAME=${{ secrets.EPA_MONGODB_USERNAME }}
            export EPA_MONGODB_PASSWORD=${{ secrets.EPA_MONGODB_PASSWORD }}
            export EPA_MONGODB_DATABASE_NAME=${{ vars.EPA_MONGODB_DATABASE_NAME }}
            export EPA_MONGODB_USER_COLLECTION=${{ vars.EPA_MONGODB_USER_COLLECTION }}
            export EPA_MONGODB_SESSION_TOKEN_COLLECTION=${{ vars.EPA_MONGODB_SESSION_TOKEN_COLLECTION }}
            export EPA_MONGODB_POSTS_COLLECTION=${{ vars.EPA_MONGODB_POSTS_COLLECTION }}
            export EPA_MONGODB_CATEGORIES_COLLECTION=${{ vars.EPA_MONGODB_CATEGORIES_COLLECTION }}
            cd ../
            
            python3 init.py
