name: "Terraform Apply Redis"

on:
  workflow_call:
    outputs:
      hostname:
        description: "The hostname of the Redis instance"
        value: ${{ jobs.terraform-apply.outputs.hostname }}
      port:
        description: "The port of the Redis instance"
        value: ${{ jobs.terraform-apply.outputs.port }}

jobs:
  terraform-apply:    
    name: "Terraform Apply Redis"
    runs-on: ubuntu-latest
    environment: production
    
    outputs:
      hostname: ${{ steps.setup-replica-set.outputs.endpoint }}
      port: ${{ steps.get-hostname-and-port.outputs.port }}
    env:
      TF_CLOUD_ORGANIZATION: "epa-terraform"
      TF_API_TOKEN: "${{ secrets.EPA_TF_API_TOKEN }}"
      TF_WORKSPACE: "terraform-github-actions-redis"
      CONFIG_DIRECTORY: "./user_timeline_post_cache/terraform"
      
    permissions: # granular permissions
      # so GitHub can check out this repo using the default github.token
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Upload Configuration
        uses: hashicorp/tfc-workflows-github/actions/upload-configuration@v1.0.0
        id: apply-upload
        with:
          workspace: ${{ env.TF_WORKSPACE }}
          directory: ${{ env.CONFIG_DIRECTORY }}

      - name: Create Apply Run
        uses: hashicorp/tfc-workflows-github/actions/create-run@v1.0.0
        id: apply-run
        with:
          workspace: ${{ env.TF_WORKSPACE }}
          configuration_version: ${{ steps.apply-upload.outputs.configuration_version_id }}

      - name: Apply
        uses: hashicorp/tfc-workflows-github/actions/apply-run@v1.0.0
        if: fromJSON(steps.apply-run.outputs.payload).data.attributes.actions.IsConfirmable
        id: apply
        with:
          run: ${{ steps.apply-run.outputs.run_id }}
          comment: "Apply Run from GitHub Actions CI ${{ github.sha }}"
          
      - name: Setup Terraform CLI
        uses: hashicorp/setup-terraform@v3
        with:
          cli_config_credentials_token: ${{ secrets.EPA_TF_API_TOKEN }}
          
      - name: Init terraform CLI
        working-directory: ${{ env.CONFIG_DIRECTORY }}
        run: terraform init
        
      - name: Get Hostname and Port
        id: get-hostname-and-port
        working-directory: ${{ env.CONFIG_DIRECTORY }}
        run: |
            echo "endpoint=$(terraform output -raw endpoint)" >> $GITHUB_OUTPUT
            echo "port=$(terraform output -raw port)" >> $GITHUB_OUTPUT