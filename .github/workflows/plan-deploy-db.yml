name: "Terraform Apply Database"

on:
  push:
    branches:
      - main

jobs:
  terraform-plan:
    if: github.event.pull_request.head.repo.full_name == github.repository
    
    name: "Terraform Plan Database"
    runs-on: ubuntu-latest
    environment: production
    env:
      TF_CLOUD_ORGANIZATION: "epa-terraform"
      TF_API_TOKEN: "${{ secrets.EPA_TF_API_TOKEN }}"
      TF_VAR_EPA_MONGODB_USERNAME: "${{ secrets.EPA_MONGODB_USERNAME }}"
      TF_VAR_EPA_MONGODB_PASSWORD: "${{ secrets.EPA_MONGODB_PASSWORD }}"
      TF_WORKSPACE: "terraform-github-actions"
      CONFIG_DIRECTORY: "./database/terraform"
    
    permissions:
      # so GitHub can check out this repo using the default github.token
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Upload Configuration
        uses: hashicorp/tfc-workflows-github/actions/upload-configuration@v1.0.0
        id: plan-upload
        with:
          workspace: ${{ env.TF_WORKSPACE }}
          directory: ${{ env.CONFIG_DIRECTORY }}
          speculative: true

      - name: Create Plan Run
        uses: hashicorp/tfc-workflows-github/actions/create-run@v1.0.0
        id: plan-run
        with:
          workspace: ${{ env.TF_WORKSPACE }}
          configuration_version: ${{ steps.plan-upload.outputs.configuration_version_id }}
          plan_only: true

      - name: Get Plan Output
        uses: hashicorp/tfc-workflows-github/actions/plan-output@v1.0.0
        id: plan-output
        with:
          plan: ${{ fromJSON(steps.plan-run.outputs.payload).data.relationships.plan.data.id }}
            
  terraform-deploy:
    if: github.event.pull_request.head.repo.full_name == github.repository
    
    name: "Terraform Apply Database"
    runs-on: ubuntu-latest
    environment: production
    env:
      TF_CLOUD_ORGANIZATION: "epa-terraform"
      TF_API_TOKEN: "${{ secrets.EPA_TF_API_TOKEN }}"
      TF_VAR_EPA_MONGODB_USERNAME: "${{ secrets.EPA_MONGODB_USERNAME }}"
      TF_VAR_EPA_MONGODB_PASSWORD: "${{ secrets.EPA_MONGODB_PASSWORD }}"
      TF_WORKSPACE: "terraform-github-actions"
      CONFIG_DIRECTORY: "./database/terraform"
      
    permissions: # granular permissions
      # so GitHub can check out this repo using the default github.token
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Upload Configuration
        uses: hashicorp/tfc-workflows-github/actions/upload-configuration@v1.0.0
        id: apply-upload
        with:
          workspace: ${{ env.TF_WORKSPACE }}
          directory: ${{ env.CONFIG_DIRECTORY }}

      - name: Create Apply Run
        uses: hashicorp/tfc-workflows-github/actions/create-run@v1.0.0
        id: apply-run
        with:
          workspace: ${{ env.TF_WORKSPACE }}
          configuration_version: ${{ steps.apply-upload.outputs.configuration_version_id }}

      - name: Apply
        uses: hashicorp/tfc-workflows-github/actions/apply-run@v1.0.0
        if: fromJSON(steps.apply-run.outputs.payload).data.attributes.actions.IsConfirmable
        id: apply
        with:
          run: ${{ steps.apply-run.outputs.run_id }}
          comment: "Apply Run from GitHub Actions CI ${{ github.sha }}"